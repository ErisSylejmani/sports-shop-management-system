import { DashboardPage } from '../../pages/DashboardPage'

/**
 * Faqja kryesore pas login — RoleBasedHome.
 * Admin / Manager / Staf: dashboard (i njëjti për tani).
 */
export function RoleBasedHome() {
  return <DashboardPage />
}
